import React from 'react';
import UserContainer from '../../containers/user_container';
const User = () => {
    return (
        <div>
            <UserContainer/>
        </div>
    );
};

export default User;