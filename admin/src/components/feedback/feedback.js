import React from 'react';
import FeedbackContainer from '../../containers/feedback_container'

const Feedback = () => {
    return (
        <div>
            <FeedbackContainer/>
        </div>
    );
};

export default Feedback;